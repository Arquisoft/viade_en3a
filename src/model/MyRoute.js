

class MyRoute {
	//gets an array of points
	constructor(id,points,name,author,description,imageSource,elevations) {
		this.id= id;
		this.points = points;
		this.name = name;
		this.author = author;
		this.description = description;
		this.imageSource = imageSource;
		this.elevations = elevations;
	}
}
export default MyRoute;