

class MyRoute {
	//gets an array of points
	constructor(id,points,name,author,description,imageSource) {
		this.id= id;
		this.points = points;
		this.name = name;
		this.author = author;
		this.description = description;
		this.imageSource = imageSource;
	}
}
export default MyRoute;